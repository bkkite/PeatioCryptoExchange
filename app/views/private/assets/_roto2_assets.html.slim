.panel.panel-default
  .panel-body
    .row
      #guide.col-xs-8
        h3 = t('.title')
        p.text-ignore = t('.intro')
      #content.col-xs-16
        .jumbotron
          .row
            .col-xs-offset-4.col-xs-16
              span.pull-left = t('.total')
              span.pull-right
                == t("currency.icon.roto2")
                = @roto2_proof.asset_sum
          .clearfix
          .row
            table.table
              thead
                tr
                  td
                    = t('.address')
                  td
                    = t('.balance')
              tbody
                - @roto2_proof.addresses.each do |entry|
                  tr
                    td
                      = link_to entry['address'], @roto2_proof.address_url(entry['address'])
                    td
                      = entry['balance']
